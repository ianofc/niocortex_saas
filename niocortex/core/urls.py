from django.urls import path
from . import views

app_name = 'core'

urlpatterns = [
    # Rotas existentes...

    # Removido: path('academico/sala/', ... name='student_lesson')
    # Removido: path('academico/disciplinas/', ... name='student_subjects')
    # Removido: path('professor/materiais/', ...)

]